application:
  location: config (GitHub)
  config:
    profile: qa
    description: valor obtenido desde 'account-service-qa.yml'

spring:
  data:
    mongodb:
      database: accounts
      port: '27017'
      host: mongo_db

logging:
  level:
    root: info
