application:
  location: config (Github)
  config:
    profile: dev
    description: valor obtenido desde 'account-dev.yml'

spring:
  data:
    mongodb:
      database: mongobd
      port: '27017'
      host: localhost

logging:
  level:
    root: info


