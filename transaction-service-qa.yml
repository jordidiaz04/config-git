application:
  location: config (GitHub)
  config:
    profile: qa
    description: valor obtenido desde 'transaction-service-qa.yml'

spring:
  data:
    mongodb:
      database: transactions
      port: '27017'
      host: mongo_db

logging:
  level:
    root: info
