application:
  location: config (GitHub)
  config:
    profile: prod
    description: valor obtenido desde 'account-service-prod.yml'

spring:
  data:
    mongodb:
      database: accounts
      port: '27017'
      host: mongo_db

logging:
  level:
    root: info
