application:
  location: config (GitHub)
  config:
    profile: qa
    description: valor obtenido desde 'fees-qa.yml'

spring:
  data:
    mongodb:
      database: fees
      port: '27017'
      host: mongo_db

logging:
  level:
    root: info
