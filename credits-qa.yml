application:
  location: config (GitHub)
  config:
    profile: qa
    description: valor obtenido desde 'credits-qa.yml'

spring:
  data:
    mongodb:
      database: credits
      port: '27017'
      host: mongo_db

logging:
  level:
    root: info
