application:
  location: config (GitHub)
  config:
    profile: local
    description: valor obtenido desde 'clients-local.yml'

spring:
  data:
    mongodb:
      database: clients
      port: '27017'
      host: localhost

logging:
  level:
    root: info
