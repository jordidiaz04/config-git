application:
  location: config (GitHub)
  config:
    profile: local
    description: valor obtenido desde 'reports-local.yml'

spring:
  data:
    mongodb:
      database: reports
      port: '27017'
      host: localhost

logging:
  level:
    root: info
