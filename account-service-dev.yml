application:
  location: config (GitHub)
  config:
    profile: dev
    description: valor obtenido desde 'account-service-dev.yml'

spring:
  data:
    mongodb:
      database: accounts
      port: '27017'
      host: localhost

logging:
  level:
    root: info
