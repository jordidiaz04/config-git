application:
  location: config (GitHub)
  config:
    profile: dev
    description: valor obtenido desde 'transaction-service-dev.yml'

spring:
  data:
    mongodb:
      database: transactions
      port: '27017'
      host: localhost

logging:
  level:
    root: info
