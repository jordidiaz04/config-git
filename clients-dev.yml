application:
  location: config (GitHub)
  config:
    profile: dev
    description: valor obtenido desde 'clients-dev.yml'

spring:
  data:
    mongodb:
      database: clients
      port: '27017'
      host: localhost

logging:
  level:
    root: info
