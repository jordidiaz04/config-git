application:
  location: config (GitHub)
  config:
    profile: dev
    description: valor obtenido desde 'credits-dev.yml'

spring:
  data:
    mongodb:
      database: credits
      port: '27017'
      host: localhost

logging:
  level:
    root: info
