application:
  location: config (GitHub)
  config:
    profile: local
    description: valor obtenido desde 'transaction-service-local.yml'

spring:
  data:
    mongodb:
      database: transactions
      port: '27017'
      host: localhost

logging:
  level:
    root: info
