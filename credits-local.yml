application:
  location: config (GitHub)
  config:
    profile: local
    description: valor obtenido desde 'credits-local.yml'

spring:
  data:
    mongodb:
      database: credits
      port: '27017'
      host: localhost

logging:
  level:
    root: info
