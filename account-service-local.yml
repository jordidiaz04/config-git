application:
  location: config (GitHub)
  config:
    profile: local
    description: valor obtenido desde 'account-service-local.yml'

spring:
  data:
    mongodb:
      database: accounts
      port: '27017'
      host: localhost

logging:
  level:
    root: info
