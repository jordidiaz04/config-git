application:
  location: config (GitHub)
  config:
    profile: qa
    description: valor obtenido desde 'transaction-qa.yml'

spring:
  data:
    mongodb:
      database: mongobd
      port: '27017'
      host: localhost

logging:
  level:
    root: info
