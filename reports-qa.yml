application:
  location: config (GitHub)
  config:
    profile: qa
    description: valor obtenido desde 'reports-qa.yml'

spring:
  data:
    mongodb:
      database: reports
      port: '27017'
      host: mongo_db

logging:
  level:
    root: info
