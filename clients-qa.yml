application:
  location: config (GitHub)
  config:
    profile: qa
    description: valor obtenido desde 'clients-qa.yml'

spring:
  data:
    mongodb:
      database: clients
      port: '27017'
      host: localhost

logging:
  level:
    root: info
