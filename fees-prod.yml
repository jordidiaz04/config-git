application:
  location: config (GitHub)
  config:
    profile: prod
    description: valor obtenido desde 'fees-prod.yml'

spring:
  data:
    mongodb:
      database: fees
      port: '27017'
      host: mongo_db

logging:
  level:
    root: info
