application:
  location: config (GitHub)
  config:
    profile: prod
    description: valor obtenido desde 'account-prod.yml'

spring:
  data:
    mongodb:
      database: accounts
      port: '27017'
      host: localhost

logging:
  level:
    root: info
