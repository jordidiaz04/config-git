application:
  location: config (GitHub)
  config:
    profile: prod
    description: valor obtenido desde 'transaction-prod.yml'

spring:
  data:
    mongodb:
      database: mongobd
      port: '27017'
      host: localhost

logging:
  level:
    root: info
